<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img src="{{item.img}}" data-edit="img" title="{{item.name}}" height="64" width="64"/>
        <h1><input name="name" type="text" value="{{item.name}}" placeholder="name"/></h1>
    </header>
    <div>
        <table>
            <tr>
                <th>{{localize "mnm3e.armor.category"}}</th>
                <th>{{localize "mnm3e.armor.effect"}}</th>
                <th>{{localize "mnm3e.armor.cost"}}</th>
            </tr>
            <tr>
                <td>
                    <select name="data.category">
                        {{#select data.category}}
                        {{#each config.armorCategories as |name category|}}
                        <option value="{{category}}">{{localize name}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </td>
                <td><input name="data.effect" type="text" value="{{data.effect}}" data-dtype="String"/></td>
                <td><input name="data.cost" type="text" value="{{data.cost}}" data-dtype="Number"/></td>
            </tr>
        </table>
        <textarea name="data.description">{{data.description}}</textarea>
    </div>
</form>